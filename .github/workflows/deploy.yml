# deploy.yml — RETIRED
# This workflow previously deployed to Azure Container Apps + ACR.
# Hosting moved to Streamlit Community Cloud (free).
# Streamlit Cloud auto-deploys on every push to main — no workflow needed.
# Grade data is now updated by fetch-grades.yml (replaces Azure Logic App).
#
# Azure resources to DELETE (one-time cleanup):
#   az containerapp delete --name ben-grade-dashboard --resource-group DeanFamily-RG --yes
#   az acr delete --name deangraderegistry --resource-group DeanFamily-RG --yes
#   az logicapp delete --name BenGrade-Automation --resource-group DeanFamily-RG --yes
#   az storage container delete --name dean-family-grades --account-name <storage-account>
